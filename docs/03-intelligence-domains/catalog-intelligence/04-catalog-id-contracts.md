# Catalog ID Contracts & Boundaries

**Document Reference:** `docs/03-intelligence-domains/catalog-intelligence/04-catalog-id-contracts.md`
**System Name:** Catalog Intelligence Domain (`Catalog ID`)
**Domain Layer:** Cognitive Intelligence Layer
**Status:** Canonical Intelligence Specification
**Authoritative Version:** 2.1
**Last Updated:** September 1, 2026

---

## 1. The Interaction Boundary

Catalog ID has **zero direct database access** to Catalog BS. It interacts strictly via authorized interfaces.

- **Discovery Phase:** Reads canonical facts via `vw_catalog_master`, `vw_catalog_products`, and `vw_catalog_skus`.
- **Command Phase:** Submits mutations via the `SaveProductFamily` contract.

---

## 2. Product Attachment Contract

The command semantics generated by Catalog ID must explicitly target one of three operations.

### 2.1 Operation: `NEW PRODUCT FAMILY`
- **Definition:** Catalog ID determines the candidate represents a distinct offering.
- **Supplied Identity:** None (`product_internal_id` omitted; Catalog BS will generate the UUID).
- **Proposed Identity:** `product_code` and `sku_id`.

### 2.2 Operation: `ATTACH NEW SKU TO EXISTING PRODUCT`
- **Definition:** Catalog ID determines the candidate is a variant of an existing family.
- **CRITICAL INVARIANT:** Catalog ID **MUST** resolve the authoritative `product_internal_id` (UUID). A matching `product_code` string is **NEVER** sufficient to attach a SKU.
- **Supplied Identity:** `product_internal_id` (mapped from `DISCOVERED_CANONICAL`).
- **Proposed Identity:** `sku_id`.

### 2.3 Operation: `UPDATE EXISTING SKU`
- **Definition:** Modifying attributes of an existing SKU.
- **Supplied Identity:** `sku_internal_id` and `product_internal_id`.
- **Proposed Identity:** None. Identifiers cannot be blindly mutated.

---

## 3. Stale Discovery Revalidation

Catalog BS is a concurrent system. Time passes between Catalog ID discovering truth, reasoning, and generating a command.

- **Requirement:** Before final mutation submission, Catalog ID **must** revalidate/re-discover the necessary current Catalog BS truth through the public read contract. An earlier discovery snapshot must not be treated as permanently authoritative.
- **Failure Handling:** If the state goes stale (e.g., a proposed `sku_id` is registered by another user in the interim), Catalog BS will deterministically reject it (`SKU_COLLISION`).

---

## 4. Failure Semantics & Rejection Handling

Catalog ID must distinguish failure modes and not collapse them into a generic "error". Catalog BS rejection is always authoritative.

| Failure Mode | Catalog ID Handling Semantic |
|---|---|
| **Cognitive Uncertainty** | (E.g., Ambiguous Candidate). Halt and escalate to human. |
| **Invalid/Incomplete Input** | (E.g., missing mandatory attributes). Reject intake or prompt user. |
| **Identity Collision** | Catalog BS `SKU_COLLISION` or `PRODUCT_CODE_COLLISION`. Trigger bounded deterministic retries, preserving the rejection reason. |
| **Validation Rejection** | (E.g., `CROSS_PRODUCT_SKU_MUTATION_DENIED`). Route to Human Escalation. Catalog ID reasoning incorrectly attempted a cross-family mutation. |
| **Stale Discovery** | Handled implicitly via Collision/Validation rejections from Catalog BS upon concurrent conflict. |
