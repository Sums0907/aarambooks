fastapi==0.104.1
uvicorn[standard]==0.24.0.post1
sqlalchemy==2.0.23
asyncpg==0.29.0
pgvector==0.2.4
pydantic==2.5.2
pydantic-settings==2.1.0
PyJWT==2.8.0
cryptography==41.0.5
httpx==0.25.1
greenlet>=3.0.0
motor
python-dotenv==1.2.3
alembic==1.20.0
# Required by src/azm/db.py's raw psycopg2.connect() (a separate sync connection AZM uses
# independently of the rest of the app's asyncpg-based access) - missing before, which made
# AZM silently fall back to a non-persistent mode in production. See
# docs/claude/DEPLOYMENT_STRATEGY_BRAIN_VPS.md's "First real deploy" section.
psycopg2-binary==2.9.9
